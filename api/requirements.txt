# Serve-only dependencies for the FastAPI Lambda image.
# Deliberately excludes xgboost/statsmodels/streamlit: the API only reads parquet
# and text from S3, so keeping it lean speeds up cold starts and image builds.
fastapi>=0.110.0
mangum>=0.17.0
pandas>=2.2.0
pyarrow>=16.0.0
numpy>=1.26.0
s3fs>=2024.6.0
fsspec>=2024.6.0
boto3>=1.34.0
python-dotenv>=1.0.1
